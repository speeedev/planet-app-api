{
  "name": "planet-app-api",
  "version": "1.0.0",
  "description": "Developed for mobile Planet app",
  "main": "index.js",
  "scripts": {
    "start": "some command to be run", // you need to add this line
    "test": "(eslint lib || true) && jest"
  }

  "author": "Emir Bolat (SpeeDEV)",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "http": "^0.0.1-security"
  }
}
